from setuptools import setup, find_packages

setup(
    name='laydown-planner',
    version='1.0.0',
    author='IfcConstructionManagerProxy',
    packages=find_packages(),
    install_requires=[
        # To be populated with content from requirements.txt
    ]
)